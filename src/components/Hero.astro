<section class="hero" id="top">
	<div class="hero__photo" aria-hidden="true"></div>
	<div class="hero__shade" aria-hidden="true"></div>

	<div class="hero__inner">
		<div class="hero__content">
			<h1 class="hero__title">РЛУС «Десна»</h1>

			<p class="hero__subtitle">
				Офіційний сайт Ради Лідерів учнівського самоврядування
				Деснянського району міста Києва
			</p>

			<a class="hero__link" href="#about">
				Дізнатися більше
				<span aria-hidden="true">↓</span>
			</a>
		</div>
	</div>
</section>

<style>
	.hero {
	position: relative;

	min-height: calc(100vh - 84px);
	overflow: hidden;

	display: flex;
	align-items: flex-start;

	background: var(--color-navy);
}

	/* Реальне фото */
	.hero__photo {
		position: absolute;
		inset: 0;

		background-image: url('/images/hero.jpg');
		background-size: cover;
		background-position: center 38%;
		background-repeat: no-repeat;

		transform: scale(1.01);
	}

	/*
		Замість сильного blur:
		зліва й знизу фото трохи затемнюється,
		щоб текст добре читався.
	*/
	.hero__shade {
		position: absolute;
		inset: 0;

		background:
			linear-gradient(
				90deg,
				rgba(9, 17, 31, 0.76) 0%,
				rgba(9, 17, 31, 0.54) 33%,
				rgba(9, 17, 31, 0.15) 68%,
				rgba(9, 17, 31, 0.04) 100%
			),
			linear-gradient(
				0deg,
				rgba(9, 17, 31, 0.64) 0%,
				rgba(9, 17, 31, 0.10) 48%,
				rgba(9, 17, 31, 0.02) 100%
			);
	}

	.hero__inner {
		position: relative;
		z-index: 2;

		width: min(calc(100% - 48px), var(--container));

		margin: 0 auto;
		padding: 105px 0 70px;
	}

	.hero__content {
	max-width: 720px;
	}

	h1 {
	margin: 0;

	display: flex;
	flex-direction: column;

	color: #ffffff;

	font-size: clamp(62px, 6vw, 105px);
	font-weight: 750;

	line-height: 0.88;
	letter-spacing: -0.055em;
}

h1 span:last-child {
	color: #ffffff;
}
	.hero__subtitle {
		max-width: 760px;

		margin: 30px 0 0;

		color: rgba(255, 255, 255, 0.9);

		font-size: clamp(18px, 1.5vw, 24px);
		font-weight: 400;

		line-height: 1.5;
	}

	.hero__link {
		display: inline-flex;
		align-items: center;
		gap: 12px;

		margin-top: 38px;
		padding-bottom: 7px;

		border-bottom: 2px solid var(--color-yellow);

		color: #ffffff;

		font-size: 15px;
		font-weight: 700;

		transition:
			color 180ms ease,
			transform 180ms ease;
	}

	.hero__link span {
		color: var(--color-yellow);
		font-size: 18px;

		transition: transform 180ms ease;
	}

	.hero__link:hover {
		color: var(--color-yellow);
		transform: translateY(-2px);
	}

	.hero__link:hover span {
		transform: translateY(3px);
	}

	@media (max-width: 800px) {
		.hero {
			min-height: calc(100svh - 84px);
		}

		.hero__photo {
			background-position: center;
		}

		.hero__shade {
			background:
				linear-gradient(
					0deg,
					rgba(9, 17, 31, 0.82) 0%,
					rgba(9, 17, 31, 0.50) 55%,
					rgba(9, 17, 31, 0.14) 100%
				);
		}

		.hero__inner {
			width: min(calc(100% - 32px), var(--container));
			padding: 90px 0 58px;
		}

		h1 {
			font-size: clamp(58px, 16vw, 92px);
		}

		.hero__subtitle {
			max-width: 600px;
			font-size: 17px;
		}
	}

	.hero__title {
	font-size: clamp(64px, 8vw, 120px);
	line-height: 0.95;
	font-weight: 800;
	white-space: nowrap;
	margin: 0;
}

@media (max-width: 900px) {
	.hero__title {
		white-space: normal;
	}

	.brand__text {
		white-space: normal;
	}
}
</style>