<header class="header">
	<div class="header__inner">
		<a class="brand" href="#top" aria-label="РЛУС Десна — головна">
			<img
	class="brand__logo"
	src="/images/desna-logo.png"
	alt="Логотип РЛУС «Десна»"
/>

			<div class="brand__text">
				<strong>РЛУС</strong>
				<span>«Десна»</span>
			</div>
		</a>

		<nav class="nav" aria-label="Головна навігація">
		<a class="news-link" href="/news">Новини</a>
<a href="/#about">Про нас</a>
<a href="/#team">Команда</a>
<a href="/#sectors">Сектори</a>
<a href="/#projects">Проєкти</a>
<a href="/#cooperation">Співпраця</a>
<a href="/#contacts">Контакти</a>
		</nav>
	</div>
</header>

<style>
	.header {
		position: sticky;
		top: 0;
		z-index: 100;

		background: rgba(255, 255, 255, 0.94);
		backdrop-filter: blur(14px);

		border-bottom: 1px solid var(--color-border);
	}

	.header__inner {
		width: min(calc(100% - 48px), var(--container));
		height: 84px;
		margin: 0 auto;

		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 48px;
	}

	.brand {
	display: flex;
	align-items: center;
	gap: 4px;
	flex-shrink: 0;
}

.brand__logo {
	width: 82px;
	height: auto;
	display: block;
	flex-shrink: 0;
	margin-right: -6px;
}

	.brand__text {
	display: flex;
	flex-direction: row;
	align-items: baseline;
	gap: 6px;
	line-height: 1;
	white-space: nowrap;
}

.brand__text strong {
	color: var(--color-blue);
	font-size: 18px;
}

.brand__text span {
	margin-top: 0;
	font-size: 18px;
	font-weight: 700;
}

	.nav {
		display: flex;
		align-items: center;
		gap: 30px;

		font-size: 15px;
		font-weight: 600;
		color: #343a46;
	}

	.nav a {
		position: relative;
		padding: 8px 0;
		transition: color 180ms ease;
	}

	.nav a::after {
		content: '';
		position: absolute;

		left: 0;
		bottom: 2px;

		width: 100%;
		height: 2px;

		background: var(--color-yellow);

		transform: scaleX(0);
		transform-origin: left;

		transition: transform 180ms ease;
	}

	.nav a:hover {
		color: var(--color-blue);
	}

	.nav a:hover::after {
		transform: scaleX(1);
	}

	@media (max-width: 950px) {
		.nav {
			display: none;
		}
	}

.news-link {
	display: inline-flex;
	align-items: center;
	gap: 8px;

	color: var(--color-blue) !important;
	font-weight: 700;

	padding: 8px 0 !important;
	margin-right: 12px;
}

.news-link::before {
	content: '';
	width: 6px;
	height: 6px;
	flex: 0 0 auto;

	border-radius: 50%;
	background: var(--color-blue);
}

.nav .news-link::after {
	background: var(--color-blue);
}

.news-link:hover {
	color: var(--color-blue) !important;
}
</style>