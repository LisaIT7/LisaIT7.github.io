<section class="sectors" id="sectors">
	<div class="sectors__inner">
		<div class="sectors__header">
			<div>
				<p class="sectors__label">Напрями роботи</p>

				<h2>
					Сектори
					<span>РЛУС «Десна»</span>
				</h2>
			</div>

			<p class="sectors__intro">
				Кожен сектор має власний напрям діяльності та об’єднує
				лідерів навколо конкретних завдань і проєктів.
			</p>
		</div>

		<div class="sectors__grid">
			<article class="sector-card sector-card--financial">
				<div class="sector-card__top">
					<div class="sector-card__icon" aria-hidden="true">
						<svg viewBox="0 0 48 48">
							<path d="M9 16h30v22H9z" />
							<path d="M15 16V11h18v5" />
							<path d="M9 24h30" />
							<circle cx="24" cy="31" r="3" />
						</svg>
					</div>

					<span class="sector-card__number">01</span>
				</div>

				<div class="sector-card__content">
					<h3>Фінансовий сектор</h3>

					<p>
						Здійснює пошук партнерів і спонсорів, організацій для
						співпраці та локацій для заходів; готує проєктні
						пропозиції, листи про співпрацю та кошториси.
					</p>
				</div>

			</article>


			<article class="sector-card sector-card--smm">
				<div class="sector-card__top">
					<a
	class="sector-card__icon sector-card__icon--link"
	href="https://www.instagram.com/desnyansky?stkn=OHdqeHdlcWEzcmln"
	target="_blank"
	rel="noopener noreferrer"
	aria-label="Відкрити Instagram РЛУС «Десна»"
>
	<svg viewBox="0 0 48 48" aria-hidden="true">
		<rect x="8" y="10" width="32" height="28" rx="6" />
		<circle cx="24" cy="24" r="7" />
		<circle cx="34" cy="16" r="2" />
	</svg>
</a>

					<span class="sector-card__number">02</span>
				</div>

				<div class="sector-card__content">
					<h3>SMM-сектор</h3>

					<p>
						Висвітлює проєкти й традиції РЛУС у соціальних (Instagram, Facebook) мережах
						та створює пізнавальний контент,
						актуальний для членів РЛУС.
					</p>
				</div>

			</article>


			<article class="sector-card sector-card--tiktok">
				<div class="sector-card__top">
					<a
	class="sector-card__icon sector-card__icon--link"
	href="https://www.tiktok.com/@desnyanskyy?_r=1&_t=ZS-99aHO4mpyuV"
	target="_blank"
	rel="noopener noreferrer"
	aria-label="Відкрити TikTok РЛУС «Десна»"
>
	<svg viewBox="0 0 48 48" aria-hidden="true">
		<rect x="7" y="8" width="34" height="32" rx="6" />
		<path d="M21 17l11 7-11 7z" />
	</svg>
</a>

					<span class="sector-card__number">03</span>
				</div>

				<div class="sector-card__content">
					<h3>TikTok-сектор</h3>

					<p>
						Популяризує ідеї лідерства та діяльність РЛУС
						через створення контенту для TikTok і висвітлення
						внутрішньої діяльності Ради.
					</p>
				</div>

			</article>
		</div>
	</div>
</section>

<style>
	.sectors {
		background: #f6f7f9;
		padding: 135px 0 80px;
	}

	.sectors__inner {
		width: min(calc(100% - 48px), var(--container));
		margin: 0 auto;
	}

	.sectors__header {
		display: grid;
		grid-template-columns: 1.15fr 0.85fr;
		gap: 70px;
		align-items: end;

		margin-bottom: 75px;
	}

	.sectors__label {
		margin: 0 0 25px;

		color: var(--color-blue);

		font-size: 12px;
		font-weight: 800;

		letter-spacing: 0.16em;
		text-transform: uppercase;
	}

	.sectors h2 {
		margin: 0;

		color: var(--color-navy);

		font-size: clamp(55px, 5.5vw, 90px);
		font-weight: 750;

		line-height: 0.96;
		letter-spacing: -0.055em;
	}

	.sectors h2 span {
		display: block;

		color: var(--color-blue);

		font-weight: 450;
		font-style: italic;
	}

.sectors__intro {
	max-width: 520px;

	margin: 0 0 10px auto;

	color: var(--color-muted);

	font-size: 20px;
	line-height: 1.65;
}

	.sectors__grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
	}

	.sector-card {
		position: relative;

		min-height: 350px;

		display: flex;
		flex-direction: column;
		justify-content: flex-start;

		padding: 34px;

		border-radius: 28px;
		border: 1px solid rgba(13, 22, 38, 0.08);

		background: #ffffff;

		overflow: hidden;

		transition:
			transform 250ms ease,
			box-shadow 250ms ease,
			border-color 250ms ease;
	}

	.sector-card:hover {
	transform: translateY(-4px);

	box-shadow:
		0 18px 45px rgba(13, 22, 38, 0.08);

	border-color: rgba(36, 87, 230, 0.16);
}

	.sector-card__top {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 20px;
	}

.sector-card__icon {
	width: 68px;
	height: 68px;
	border-radius: 20px;
	display: flex;
	align-items: center;
	justify-content: center;

	background: linear-gradient(
		145deg,
		#fffdf4 0%,
		#f8efc9 100%
	);
} 

	.sector-card__icon svg {
		width: 32px;
		height: 32px;

		fill: none;
		stroke: currentColor;
		stroke-width: 2;
		stroke-linecap: round;
		stroke-linejoin: round;
	}

	.sector-card__number {
		color: #9ca1aa;

		font-size: 11px;
		font-weight: 800;

		letter-spacing: 0.12em;
	}

	.sector-card__content {
		margin-top: 55px;
		padding-top: 0;
	}

	.sector-card h3 {
		margin: 0;

		color: var(--color-navy);

		font-size: 31px;
		font-weight: 700;

		line-height: 1.05;
		letter-spacing: -0.035em;
	}

	.sector-card p {
		margin: 20px 0 0;

		color: var(--color-muted);

		font-size: 16px;
		line-height: 1.65;
	}



	/* Маленькі відмінності між картками */
.sector-card--financial {
	background:
		linear-gradient(
			145deg,
			#ffffff 0%,
			#eaeffe 100%
		);
}

.sector-card--smm {
	background:
		linear-gradient(
			145deg,
			#ffffff 0%,
			#eaeffe 100%
		);
}

.sector-card--tiktok {
	background:
		linear-gradient(
			145deg,
			#ffffff 0%,
			#e1e8fd 100%
		);
}


.sector-card--tiktok h3 {
	color: var(--color-navy);
}

.sector-card--tiktok p {
	color: var(--color-muted);
}

.sector-card--tiktok .sector-card__number {
	color: #9ca1aa;
}


	.sector-card--tiktok .sector-card__arrow {
		color: var(--color-yellow);
	}

	.sector-card__icon--link {
	text-decoration: none;
	cursor: pointer;

	transition:
		transform 180ms ease,
		box-shadow 180ms ease;
}

.sector-card__icon--link:hover {
	transform: translateY(-3px) scale(1.04);

	box-shadow:
		0 10px 24px rgba(13, 22, 38, 0.10);
}

.sector-card__icon--link:focus-visible {
	outline: 2px solid var(--color-blue);
	outline-offset: 4px;
}

	@media (max-width: 1000px) {
		.sectors__header {
			grid-template-columns: 1fr;
			gap: 35px;
		}

		.sectors__intro {
			margin-left: 0;
		}

		.sectors__grid {
			grid-template-columns: 1fr;
		}

		.sector-card {
			min-height: 380px;
		}
	}

	@media (max-width: 600px) {
		.sectors {
			padding: 90px 0;
		}

		.sectors__inner {
			width: min(calc(100% - 32px), var(--container));
		}

		.sector-card {
			min-height: 350px;
			padding: 27px;
			border-radius: 22px;
		}

		.sector-card h3 {
			font-size: 28px;
		}
	}

    .sector-card--financial .sector-card__icon,
.sector-card--smm .sector-card__icon,
.sector-card--tiktok .sector-card__icon {
	background:
		linear-gradient(
			145deg,
			#fffdf4 0%,
			#f8efc9 100%
		);

	color: var(--color-navy);
}

</style>